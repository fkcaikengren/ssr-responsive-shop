class PageView {
  a = 'love';

  b = 1;

  calcArea() {
    return this.name;
  }
}

export const pageview = new PageView();

// 以api开头的url 会交给localhost:4000进行代理 以api开头的url 会交给localhost:4000进行代理  以api开头的url 会交给localhost:4000进行代理 定义为使用
